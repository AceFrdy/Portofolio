// import nodemailer from "nodemailer";

// const email = process.env.EMAIL; // email kamu, misal: your-email@gmail.com
// const pass = process.env.EMAIL_PASS; // app password (kalau pakai Gmail, gunakan app password)

// export const transporter = nodemailer.createTransport({
//   service: "gmail",
//   auth: {
//     user: email,
//     pass,
//   },
// });

// export const mailOptions = {
//   from: email, // sender yang muncul di email keluar (biasanya email portofolio kamu)
//   to: email,   // email tujuan, biar pesan dari form langsung masuk ke inbox kamu
// };
